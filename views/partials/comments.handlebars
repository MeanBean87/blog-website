<div class="comments">
    {{#each this}}
    <section class="comment">
        <div>
            {{user.user_name}} commented on {{format_date created_at}}
        </div>
        <div>
            {{comment_content}}
        </div>
    </section>
    {{/each}}
</div>