<div class="comments">
    {{#each this}}
    <section class="comment">
        <div>
            {{user.user_id}} on {{formatDate created_at}}
        </div>
        <div>
            {{comment_content}}
        </div>
    </section>
    {{/each}}
</div>