<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.user_id}} on {{formatDate created_at}}
        </div>
        <div class="text">
            {{comment_content}}
        </div>
    </section>
    {{/each}}
</div>